<Window x:Class="BMRM.Desktop.Views.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:prism="http://prismlibrary.com/"
        xmlns:b="http://schemas.microsoft.com/xaml/behaviors"
        xmlns:wpf="clr-namespace:CefSharp.Wpf;assembly=CefSharp.Wpf"
        xmlns:shell="clr-namespace:BMRM.Desktop.Views.Shell"
        xmlns:utils="clr-namespace:BMRM.Desktop.Utils"
        Title="{Binding Title}"
        Height="600" Width="800"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent"
        ResizeMode="CanResizeWithGrip">

    <Window.Resources>
        <utils:EnumDisplayConverter x:Key="EnumDisplayConverter" />
        <LinearGradientBrush x:Key="MainBackgroundBrush" StartPoint="0,0" EndPoint="1,1">
            <GradientStop Color="#FF1E1E1E" Offset="0" />
            <GradientStop Color="#FF3B1F00" Offset="1" />
        </LinearGradientBrush>

        <Style TargetType="Button">
            <Setter Property="Background" Value="#FF6F00" />
            <Setter Property="Foreground" Value="White" />
            <Setter Property="FontFamily" Value="Segoe UI" />
            <Setter Property="FontWeight" Value="SemiBold" />
            <Setter Property="Padding" Value="10,5" />
            <Setter Property="BorderThickness" Value="0" />
            <Setter Property="Cursor" Value="Hand" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Height="30" Background="{TemplateBinding Background}" CornerRadius="4">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" />
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style TargetType="ListBox">
            <Setter Property="Background" Value="#2C2C2C" />
            <Setter Property="Foreground" Value="#E0E0E0" />
            <Setter Property="FontFamily" Value="Segoe UI" />
            <Setter Property="BorderThickness" Value="0" />
        </Style>

        <Style x:Key="TrackItemTemplate" TargetType="ItemsControl">
            <Setter Property="ItemTemplate">
                <Setter.Value>
                    <DataTemplate>
                        <Border x:Name="ItemBorder"
                                Background="#2C2C2C"
                                CornerRadius="4"
                                Margin="4"
                                Padding="8">
                            <Grid>
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="Auto" />
                                    <RowDefinition Height="Auto" />
                                </Grid.RowDefinitions>
                                <Grid Height="40">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*" />
                                        <ColumnDefinition Width="*" />
                                        <ColumnDefinition Width="*" />
                                        <ColumnDefinition Width="*" />
                                        <ColumnDefinition Width="*" />
                                        <ColumnDefinition Width="*" />
                                    </Grid.ColumnDefinitions>

                                    <TextBlock Grid.Column="0" Text="{Binding Artist}" Foreground="#E0E0E0"
                                               FontSize="14" />
                                    <TextBlock Grid.Column="1"
                                               Text="{Binding Type, Converter={StaticResource EnumDisplayConverter}}"
                                               Foreground="#9E9E9E" FontSize="13" />
                                    <TextBlock Grid.Column="2" Text="{Binding Title}" Foreground="#9E9E9E"
                                               FontSize="13" />
                                    <TextBlock Grid.Column="3"
                                               Text="{Binding ProcessingStatus, Converter={StaticResource EnumDisplayConverter}}"
                                               FontSize="13">
                                        <TextBlock.Style>
                                            <Style TargetType="TextBlock">
                                                <Setter Property="Foreground" Value="#9E9E9E" />

                                                <Style.Triggers>
                                                    <DataTrigger Binding="{Binding ProcessingStatus}" Value="New">
                                                        <Setter Property="Foreground" Value="Orange" />
                                                    </DataTrigger>

                                                    <DataTrigger Binding="{Binding ProcessingStatus}"
                                                                 Value="FoundOnSpotify">
                                                        <Setter Property="Foreground" Value="Green" />
                                                    </DataTrigger>

                                                    <DataTrigger Binding="{Binding ProcessingStatus}"
                                                                 Value="NotFoundOnSpotify">
                                                        <Setter Property="Foreground" Value="Red" />
                                                    </DataTrigger>
                                                </Style.Triggers>
                                            </Style>
                                        </TextBlock.Style>
                                    </TextBlock>
                                    <TextBlock Grid.Column="4" Text="{Binding Genres}" Foreground="#9E9E9E"
                                               FontSize="13" />
                                    <TextBlock Grid.Column="5" Text="{Binding City}" Foreground="#9E9E9E" FontSize="13" />
                                </Grid>
                                <ItemsControl Grid.Row="1" ItemsSource="{Binding Tracks}">
                                    <ItemsControl.ItemTemplate>
                                        <DataTemplate>
                                            <Grid x:Name="AlbumItem">
                                                <Grid.ColumnDefinitions>
                                                    <ColumnDefinition Width="*" />
                                                </Grid.ColumnDefinitions>
                                                <TextBlock Grid.Column="4" Text="{Binding Name}" Foreground="#9E9E9E"
                                                           FontSize="13" />
                                                <wpf:ChromiumWebBrowser Width="300" Height="200">
                                                    <wpf:ChromiumWebBrowser.Address>
                                                        <MultiBinding
                                                            StringFormat="https://open.spotify.com/embed/track/{0}?theme=0">
                                                            <Binding Path="Id" />
                                                        </MultiBinding>
                                                    </wpf:ChromiumWebBrowser.Address>
                                                </wpf:ChromiumWebBrowser>
                                            </Grid>
                                        </DataTemplate>
                                    </ItemsControl.ItemTemplate>
                                </ItemsControl>
                            </Grid>
                        </Border>

                    </DataTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>


    <shell:ShellControl Title="{Binding Title}">
        <shell:ShellControl.Content>
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="*" />
                    <RowDefinition Height="Auto" />
                </Grid.RowDefinitions>
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="*" />
                    </Grid.ColumnDefinitions>

                    <Button Grid.Column="0" Command="{Binding UpdateCommand}" Content="Update" Margin="10" />
                    <Button Grid.Column="1" Command="{Binding LinkedReleasesCommand}" Content="Linked Releases"
                            Margin="10" />

                </Grid>
                <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto">
                    <ItemsControl ItemsSource="{Binding Tracks}" Style="{StaticResource TrackItemTemplate}" />
                </ScrollViewer>
                <Grid Grid.Row="2">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*" />
                    </Grid.ColumnDefinitions>

                    <Button  Command="{Binding UpdatePlaylistCommand}" Content="Update playlist"
                            Margin="10" />

                </Grid>
            </Grid> 
        </shell:ShellControl.Content>
    </shell:ShellControl>
</Window>